#include "Link_list.h"


bool insert(int value,Link_List* A){
       bool is_insert=false;
       
       return is_insert;
}
bool delete_by_value(int value);
bool delete_by_place(int index);
bool change_by_place(int index);
bool change_by_value(int value);